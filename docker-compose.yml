version: '3.8'

services:
  langflowai:
    # Esta seção continua igual, pois a customização do Langflow é necessária
    build:
      context: .
      dockerfile: Dockerfile.langflow
    restart: unless-stopped
    command: uvicorn langflow.main:create_app --host 0.0.0.0 --port 7860 --workers 4
    environment:
      # --- As variáveis continuam as mesmas ---
      - LANGFLOW_NEW_USER_IS_ACTIVE=true
      - LANGFLOW_SUPERUSER=${LANGFLOW_SUPERUSER}
      - LANGFLOW_SUPERUSER_PASSWORD=${LANGFLOW_SUPERUSER_PASSWORD}
      - TZ=America/Sao_Paulo
      - LANG=pt_BR.UTF-8
      - LANGUAGE=pt_BR:pt:en
      - LC_ALL=pt_BR.UTF-8
      - LANGFLOW_DATABASE_URL=${LANGFLOW_DATABASE_URL}
      - LANGFLOW_CACHE_TYPE=redis
      - LANGFLOW_REDIS_URL=${LANGFLOW_REDIS_URL}
      - AWS_S3_ENDPOINT_URL=${AWS_S3_ENDPOINT_URL}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_S3_REGION=auto
    depends_on:
      - postgres
      - redis
    networks:
      - sua_rede_backend # Use a mesma rede dos outros serviços

  postgres:
    # --- MUDANÇA AQUI ---
    # Removemos o 'build' e voltamos a usar a imagem oficial.
    # A variável TZ é suficiente para o Postgres.
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      - TZ=America/Sao_Paulo
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - sua_rede_backend

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    networks:
      - sua_rede_backend

volumes:
  postgres_data: {}

networks:
  sua_rede_backend:
    # ...